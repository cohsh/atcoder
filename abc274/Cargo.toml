[package]
name = "abc274"
version = "0.1.0"
edition = "2021"

# dependencies added to new project
[dependencies]
proconio = { version = "*", features = ["derive"] }

[profile.release]
lto = true
panic = 'abort'
